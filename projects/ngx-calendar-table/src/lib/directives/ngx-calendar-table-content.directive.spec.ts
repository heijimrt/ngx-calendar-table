import { NgxCalendarTableContentDirective } from './ngx-calendar-table-content.directive';

describe('NgxCalendarTableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCalendarTableContentDirective();
    expect(directive).toBeTruthy();
  });
});
